import PlaceholderPage from '../components/PlaceholderPage'

export default function RosterPage() {
  return (
    <PlaceholderPage
      step={3}
      title="Roster & student detail"
      description="Student list, attributes, recurring slots editor, and the notes panel."
    />
  )
}
