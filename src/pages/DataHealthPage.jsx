import PlaceholderPage from '../components/PlaceholderPage'

export default function DataHealthPage() {
  return (
    <PlaceholderPage
      step={9}
      title="Data health"
      description="Missing attributes, unlinked import names, and sessions flagged for review."
    />
  )
}
