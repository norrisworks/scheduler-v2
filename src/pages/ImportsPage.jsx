import PlaceholderPage from '../components/PlaceholderPage'

export default function ImportsPage() {
  return (
    <PlaceholderPage
      step="7–8"
      title="Imports"
      description="Radius sessions (xlsx) and Workstream shifts (csv), each with a diff preview before commit."
    />
  )
}
